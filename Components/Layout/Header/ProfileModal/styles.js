// ** MUI Imports
import { makeStyles } from "tss-react/mui";

const useStyles = makeStyles()((theme) => {
  return {
    container: {
      width: "250px",
    },
    btn: {
      padding: "20px 16px",
    },
  };
});

export default useStyles;
